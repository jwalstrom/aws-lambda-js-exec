{
  "name": "aws-lambda-js-exec",
  "version": "1.0.0",
  "description": "Extend you application by executing user custom scripts using AWS Lambda as a sandbox.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.69.0"
  }
}
